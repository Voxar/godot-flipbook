extends SpotLight3D


#func _ready():
	#var vpt = ($"../Viewport" as SubViewport).get_texture();
	#light_projector = vpt;

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(_delta):
#	pass
